name: GitOps-Automatic-Flow
on: [push]
jobs:
  sincronizzazione-layer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Esecuzione Comandi Cloud
        run: |
          echo "Agente GitOps attivato!"
